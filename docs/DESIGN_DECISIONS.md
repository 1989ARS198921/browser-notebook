# Дизайн-решения и идеи проекта browser-notebook

## 🧠 Идеи и цели

1.  **Кроссплатформенность:** Приложение должно работать в любом современном браузере, будь то десктоп или мобильное устройство.
2.  **Простота и удобство:** Интерфейс должен быть интуитивно понятным и удобным для ежедневного использования.
3.  **Масштабируемость:** Архитектура должна позволять легко добавлять новые функции.
4.  **Локальное хранение:** Данные хранятся локально на сервере (например, Raspberry Pi), обеспечивая конфиденциальность.
5.  **Мощный редактор:** Для статей нужен полноценный WYSIWYG-редактор.
6.  **Публикация контента:** Возможность отмечать записи как "опубликованные" для просмотра не-администраторами.
7.  **Управление пользователями:** Чёткое разделение на администратора и обычного пользователя.

## 🛠️ Выбор технологий

*   **Flask:** Легковесный и гибкий микрофреймворк для быстрой разработки веб-приложений на Python.
*   **SQLAlchemy:** ORM для удобной работы с базой данных.
*   **SQLite:** Встраиваемая база данных, идеально подходящая для локального хранения данных без необходимости в отдельном сервере БД.
*   **Flask-Login:** Простая и надёжная система аутентификации пользователей.
*   **TinyMCE:** Мощный и настраиваемый WYSIWYG-редактор для форматирования статей.
*   **CSS/JS:** Для создания адаптивного и интерактивного интерфейса.
*   **PWA:** Для возможности установки приложения на устройства как нативное.

## 🧱 Архитектурные решения

*   **Роли пользователей:** Введена роль администратора (`is_admin`), которая получает доступ к полному функционалу (создание, редактирование, удаление, публикация). Обычные пользователи могут только просматривать опубликованный контент.
*   **Поле `is_published`:** Добавлено в модель `Note` для контроля видимости записей не-админам.
*   **Разделение маршрутов:** Отдельные маршруты для админ-панели (`/admin`) и публичного просмотра (`/`, `/wall`, `/public_articles`).
*   **PEP 263:** Добавлена декларация кодировки `# -*- coding: utf-8 -*-` в `app.py` и `models.py` для корректной работы с кириллическими строками в коде.
*   **Темы интерфейса:** Реализована возможность переключения между светлой, тёмной и системной темой с помощью CSS-переменных и JavaScript.