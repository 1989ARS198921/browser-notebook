# Архитектура проекта browser-notebook

## 📁 Структура файлов
notebook_app/
├── app.py # Основной файл приложения Flask. Содержит маршруты, настройки, логику аутентификации.
├── models.py # Модели данных SQLAlchemy (Note, Category, User).
├── requirements.txt # Зависимости Python.
├── static/ # Статические файлы.
│ ├── css/
│ │ └── style.css # Основные стили приложения.
│ ├── js/
│ │ └── theme-toggle.js # Скрипт для переключения тем.
│ ├── tinymce/ # (Опционально) Файлы редактора TinyMCE.
│ ├── uploads/ # (Создаётся автоматически) Загруженные изображения.
│ └── icons/ # Иконки приложения.
└── templates/ # HTML-шаблоны Jinja2.
├── base.html # Базовый шаблон.
├── index.html # Главная админ-панели.
├── wall.html # Публичная стена записей.
├── edit.html # Форма редактирования заметки.
├── edit_article.html # Форма редактирования статьи.
├── read_article.html # Режим чтения статьи.
├── login.html # Страница входа.
├── register.html # Страница регистрации.
└── ...

## 🏗️ Основные компоненты

### `app.py`

*   **Flask Application:** Создаёт экземпляр приложения `Flask`.
*   **Configuration:** Настройки SECRET_KEY, DATABASE_URI, UPLOAD_FOLDER.
*   **Flask-Login Setup:** Инициализация `LoginManager`, функция `user_loader`.
*   **Database Initialization:** Инициализация `SQLAlchemy` и создание таблиц.
*   **Routes:** Определение маршрутов (`@app.route`) для различных страниц и действий (создание, редактирование, удаление, вход, выход).
*   **Authentication Logic:** Маршруты `/login`, `/logout`, `/register`.
*   **Content Management Logic:** Маршруты `/admin`, `/edit`, `/edit_article`, `/wall`, `/public_articles`, `/read_article` и т.д.
*   **Image Upload Handler:** Маршрут `/upload_image` для TinyMCE.
*   **PEP 263:** Декларация `# -*- coding: utf-8 -*-`.

### `models.py`

*   **SQLAlchemy Models:**
    *   `User`: Модель пользователя с полями `username`, `password_hash`, `is_admin`.
    *   `Note`: Модель записи с полями `title`, `content`, `full_content`, `note_type`, `is_published`, `category_ids`, `tags`, `background_color`, `preview_image`, `image_filename`.
    *   `Category`: Модель категории.
    *   `note_categories`: Таблица связи многие-ко-многим между `Note` и `Category`.
*   **PEP 263:** Декларация `# -*- coding: utf-8 -*-`.

### `templates/`

*   **Jinja2 Templates:** Используют наследование (`{% extends %}`), блоки (`{% block %}`), условия (`{% if %}`), циклы (`{% for %}`).
*   **Base Template (`base.html`):** Общая структура страницы (шапка, боковая панель, подвал).
*   **Specific Templates:** Отдельные шаблоны для каждой страницы (список, редактирование, чтение, вход и т.д.).

### `static/css/style.css`

*   **CSS Variables:** Для реализации светлой/тёмной темы.
*   **Responsive Design:** Использование `flexbox`, `grid`, `media queries`.
*   **Modern UI:** Чистые стили, скругления, тени, плавные переходы.
*   **PEP 263:** Декларация `# -*- coding: utf-8 -*-` (опционально, но для консистентности).

### `static/js/theme-toggle.js`

*   **JavaScript Logic:** Для переключения темы интерфейса.
*   **localStorage:** Сохранение выбора темы пользователя.
*   **matchMedia:** Учёт системной темы браузера.